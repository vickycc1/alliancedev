# TechCommunity 前端测试清单

## 一、认证与授权模块

### 1.1 登录页 (Login)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| AUTH-001 | 空用户名提交 → 表单校验提示"请输入用户名" | P0 | 表单校验 | ✅ 通过 | `rules=[{required:true, message:'请输入用户名'}]` |
| AUTH-002 | 空密码提交 → 表单校验提示"请输入密码" | P0 | 表单校验 | ✅ 通过 | `rules=[{required:true, message:'请输入密码'}]` |
| AUTH-003 | 正确用户名密码登录 → 跳转首页，localStorage 存储 token/refreshToken/expiresIn | P0 | 正向流程 | ✅ 通过 | `setTokens()` 写入3项到 localStorage，`navigate(redirect)` |
| AUTH-004 | 错误用户名密码登录 → 提示"登录失败，请检查用户名和密码" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('登录失败，请检查用户名和密码')` |
| AUTH-005 | 登录中按钮 loading 状态，防止重复提交 | P1 | 交互 | ✅ 通过 | `useState(false)` + `setLoading(true/false)` + `Button loading={loading}` |
| AUTH-006 | 登录成功后 redirect 参数跳转 → 访问 `/login?redirect=/new-post` 登录后跳转到 `/new-post` | P0 | 路由守卫 | ✅ 通过 | `useSearchParams` 读取 redirect，`navigate(redirect, {replace:true})` |
| AUTH-007 | 已登录用户访问 `/login` → 自动跳转首页（GuestGuard） | P0 | 路由守卫 | ✅ 通过 | GuestGuard 检测 `isAuthenticated` → `Navigate to='/'` |
| AUTH-008 | 后端返回 401 → 自动跳转登录页并携带 redirect | P0 | 异常流程 | ✅ 通过 | `handleUnauthorized()` → `window.location.href=/login?redirect=...` |

### 1.2 注册页 (Register)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| REG-001 | 用户名少于3字符 → 提示"用户名至少3个字符" | P0 | 表单校验 | ✅ 通过 | `{min:3, message:'用户名至少3个字符'}` |
| REG-002 | 用户名含特殊字符（非字母数字下划线）→ 提示"用户名只能包含字母、数字和下划线" | P0 | 表单校验 | ✅ 通过 | `{pattern:/^[a-zA-Z0-9_]+$/, message:'用户名只能包含字母、数字和下划线'}` |
| REG-003 | 用户名超过50字符 → 提示"用户名最多50个字符" | P1 | 表单校验 | ✅ 通过 | `{max:50, message:'用户名最多50个字符'}` |
| REG-004 | 空昵称提交 → 提示"请输入昵称" | P0 | 表单校验 | ✅ 通过 | `{required:true, message:'请输入昵称'}` |
| REG-005 | 无效邮箱格式 → 提示"请输入有效的邮箱地址" | P0 | 表单校验 | ✅ 通过 | `{type:'email', message:'请输入有效的邮箱地址'}` |
| REG-006 | 密码少于6字符 → 提示"密码至少6个字符" | P0 | 表单校验 | ✅ 通过 | `{min:6, message:'密码至少6个字符'}` |
| REG-007 | 确认密码与密码不一致 → 提示"两次输入的密码不一致" | P0 | 表单校验 | ✅ 通过 | 自定义 validator 比对 `getFieldValue('password')` |
| REG-008 | 注册成功 → 提示"注册成功，请登录"并跳转 `/login` | P0 | 正向流程 | ✅ 通过 | `message.success('注册成功，请登录')` + `navigate('/login', {replace:true})` |
| REG-009 | 注册失败（用户名已存在等）→ 提示"注册失败，请稍后重试" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('注册失败，请稍后重试')` |
| REG-010 | "已有账号？立即登录"链接 → 跳转 `/login` | P1 | 导航 | ✅ 通过 | `<Link to="/login">立即登录</Link>` |

### 1.3 路由守卫
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| GUARD-001 | 未登录访问需认证页面（`/new-post`, `/profile`, `/notifications`）→ 跳转 `/login?redirect=原路径` | P0 | AuthGuard | ✅ 通过 | AuthGuard: `Navigate to={/login?redirect=${encodeURIComponent(location.pathname)}}` |
| GUARD-002 | 已登录访问需认证页面 → 正常渲染 | P0 | AuthGuard | ✅ 通过 | `isAuthenticated` 为 true 时 `return <>{children}</>` |
| GUARD-003 | AuthGuard loading 状态 → 显示 Spin 加载 | P1 | AuthGuard | ✅ 通过 | `if(loading) return <Spin size="large" />` |
| GUARD-004 | 非管理员访问 `/admin/*` → 跳转首页 `/` | P0 | AdminGuard | ✅ 通过 | `if(!user?.roles.includes(RoleCode.ADMIN)) return <Navigate to="/" />` |
| GUARD-005 | 管理员访问 `/admin/*` → 正常渲染 | P0 | AdminGuard | ✅ 通过 | 角色校验通过后 `return <>{children}</>` |
| GUARD-006 | 未登录访问 `/admin/*` → 跳转登录页 | P0 | AdminGuard | ✅ 通过 | 先检查 `isAuthenticated`，未登录跳转 `/login?redirect=...` |
| GUARD-007 | 角色不匹配 → RoleGuard 跳转首页 | P1 | RoleGuard | ✅ 通过 | `user?.roles.some(...roles)` 不匹配时 `Navigate to="/"` |
| GUARD-008 | 已登录访问 `/login` 或 `/register` → GuestGuard 跳转首页或 redirect | P0 | GuestGuard | ✅ 通过 | `if(isAuthenticated) return <Navigate to={redirect} />` |

### 1.4 Token 管理
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| TOKEN-001 | 登录成功 → localStorage 存储 token、refreshToken、expiresIn | P0 | 存储 | ✅ 通过 | `setTokens()` 写入3个 key 到 localStorage |
| TOKEN-002 | 请求拦截器自动附加 `Authorization: Bearer <token>` | P0 | 拦截器 | ✅ 通过 | 请求拦截器: `config.headers.Authorization = 'Bearer ' + token` |
| TOKEN-003 | Token 过期 → 自动使用 refreshToken 刷新 | P0 | 刷新机制 | ✅ 通过 | 响应拦截器 401 处理: `axios.post('/api/auth/refresh', {refreshToken})` |
| TOKEN-004 | Token 刷新期间并发请求排队等待新 token | P1 | 并发处理 | ✅ 通过 | `isRefreshing` + `pendingRequests` 队列机制 |
| TOKEN-005 | refreshToken 也失效 → 清除 token 并跳转登录页 | P0 | 异常流程 | ✅ 通过 | 刷新失败时调用 `handleUnauthorized()` |
| TOKEN-006 | 退出登录 → 清除 localStorage 中的 token 相关项 | P0 | 清理 | ✅ 通过 | `clearTokens()` 移除3个 key |
| TOKEN-007 | `isTokenExpired()` 正确判断 token 是否过期 | P1 | 工具函数 | ✅ 通过 | 比较 `Date.now()` 与 `localStorage.getItem(TOKEN_EXPIRES_KEY)` |

---

## 二、帖子浏览模块

### 2.1 首页 (Home / PostListPage)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| HOME-001 | 页面加载 → 请求 `/posts` 并渲染帖子列表 | P0 | 数据加载 | ✅ 通过 | `useEffect` → `fetchPosts()` 请求 `/posts` |
| HOME-002 | 分类 Tab 切换 → 请求对应分类帖子，重置页码 | P0 | 筛选 | ✅ 通过 | `handleCategoryChange` → `setActiveCategory` + `setPage(1)` |
| HOME-003 | 排序切换（最新/最热/评论最多）→ 重新请求并重置页码 | P0 | 排序 | ✅ 通过 | `handleSortChange` → `setSortBy` + `setPage(1)` |
| HOME-004 | 搜索框输入关键词搜索 → 请求带 keyword 参数 | P0 | 搜索 | ✅ 通过 | `handleSearch` → `setKeyword` + `setPage(1)`，fetchPosts 带入 params |
| HOME-005 | 清除搜索标签 → 重置 keyword 并刷新列表 | P1 | 交互 | ✅ 通过 | `Tag closable onClose={() => {setKeyword(''); setPage(1)}}` |
| HOME-006 | 重置按钮 → 清空所有筛选条件（关键词、分类、排序、精华） | P1 | 交互 | ✅ 通过 | `handleRefresh` 重置所有状态到默认值 |
| HOME-007 | 分页切换 → 请求对应页数据 | P0 | 分页 | ✅ 通过 | `Pagination onChange={(p) => setPage(p)}` |
| HOME-008 | 空帖子列表 → 显示 Empty 组件"暂无帖子" | P1 | 空状态 | ✅ 通过 | `posts.length === 0 ? <Empty description="暂无帖子" />` |
| HOME-009 | 加载中 → 显示 Spin 加载 | P1 | 加载状态 | ✅ 通过 | `loading ? <Spin size="large" />` |
| HOME-010 | 分类树加载失败 → 不崩溃，分类 Tab 仍显示"全部" | P2 | 容错 | ✅ 通过 | 请求无 catch 处理但默认 `categories=[]`，Tab 始终有"全部"项 |

### 2.2 热门页 (Hot)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| HOT-001 | 默认排序为 `SortBy.HOTTEST` | P0 | 默认参数 | ✅ 通过 | `<PostListPage defaultSortBy={SortBy.HOTTEST} />` |
| HOT-002 | 标题显示"热门帖子"带火焰图标 | P1 | UI | ✅ 通过 | `title="热门帖子"` + `<FireOutlined />` |

### 2.3 精华页 (Essence)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| ESS-001 | 默认 `isEssence=true`，仅显示精华帖 | P0 | 默认参数 | ✅ 通过 | `<PostListPage defaultIsEssence />` |
| ESS-002 | 标题显示"精华帖子"带星标图标 | P1 | UI | ✅ 通过 | `title="精华帖子"` + `<StarOutlined />` |
| ESS-003 | 关闭精华标签后 → 恢复显示全部帖子 | P1 | 交互 | ✅ 通过 | PostListPage 中精华 Tag `onClose={() => {setIsEssence(false)}}` |

### 2.4 分类页 (Category)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| CAT-001 | URL 参数 `/category/:id` 正确传递 categoryId | P0 | 路由参数 | ✅ 通过 | `useParams()` 取 `id`，传入 `defaultCategoryId` |
| CAT-002 | 无效分类 ID → 回退到 categoryId=0（全部） | P1 | 容错 | ✅ 通过 | `Number(id) || 0`，PostListPage 中 `allIds.has()` 校验 |

### 2.5 帖子卡片 (PostCard)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| CARD-001 | 点击卡片 → 跳转 `/post/{id}` | P0 | 导航 | ✅ 通过 | `onClick → navigate(/post/${post.id})` |
| CARD-002 | 置顶帖显示红色"置顶"标签 | P1 | 标签 | ✅ 通过 | `post.isTop===1 && <Tag color="red">置顶</Tag>` |
| CARD-003 | 精华帖显示金色"精华"标签 | P1 | 标签 | ✅ 通过 | `post.isEssence===1 && <Tag color="gold">精华</Tag>` |
| CARD-004 | AI 帖显示紫色"AI"标签 | P1 | 标签 | ✅ 通过 | `post.aiRequested===1 && <Tag color="purple">AI</Tag>` |
| CARD-005 | 已屏蔽帖显示红色"已屏蔽"标签 | P1 | 标签 | ✅ 通过 | `statusTagMap[PostStatus.BLOCKED] = {label:'已屏蔽', color:'red'}` |
| CARD-006 | 摘要超过2行自动省略 | P2 | UI | ✅ 通过 | `<Paragraph ellipsis={{rows:2}}>` |
| CARD-007 | hover 状态显示阴影和蓝色边框 | P2 | 交互 | ✅ 通过 | `onMouseEnter` → `boxShadow` + `borderColor: '#1677FF'` |
| CARD-008 | 显示浏览量、点赞数、评论数、收藏数 | P1 | 数据展示 | ✅ 通过 | `viewCount/likeCount/commentCount/favoriteCount` 均渲染 |

### 2.6 帖子详情页 (Post)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| POST-001 | 页面加载 → 请求 `/posts/{id}` 渲染详情 | P0 | 数据加载 | ✅ 通过 | `useEffect` → `fetchPost()` 请求 `/posts/${id}` |
| POST-002 | 帖子不存在或加载失败 → 提示"获取帖子失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('获取帖子失败')`，`!post` 显示"帖子不存在" |
| POST-003 | 置顶/精华/AI 标签正确显示 | P1 | 标签 | ✅ 通过 | 与 PostCard 相同逻辑 |
| POST-004 | 作者头像、昵称、发布时间正确显示 | P1 | 数据展示 | ✅ 通过 | `<Avatar>` + `<Text>` + `toLocaleString('zh-CN')` |
| POST-005 | 编辑时间与创建时间不同时显示"编辑于 xxx" | P2 | UI | ✅ 通过 | `post.updatedAt !== post.createdAt && ' · 编辑于 ...'` |
| POST-006 | 帖子内容正确渲染（Markdown） | P1 | 内容渲染 | ⚠️ 部分通过 | 简易 Markdown 解析（#标题/列表/代码），非完整 Markdown 渲染器，不支持表格、嵌套代码块等 |
| POST-007 | 点赞按钮交互（见 LikeButton 测试） | P0 | 交互 | ✅ 通过 | 引用 LikeButton 组件 |
| POST-008 | 收藏按钮交互（见 FavoriteButton 测试） | P0 | 交互 | ✅ 通过 | 引用 FavoriteButton 组件 |
| POST-009 | 分享按钮交互（见 ShareButton 测试） | P1 | 交互 | ✅ 通过 | 引用 ShareButton 组件 |
| POST-010 | AI 回答卡片显示（见 AIReplyCard 测试） | P1 | 功能 | ✅ 通过 | `post.aiRequested===1 && <AIReplyCard />` |
| POST-011 | 评论列表显示（见 CommentList 测试） | P0 | 功能 | ✅ 通过 | `<CommentList postId={post.id} />` |
| POST-012 | 作者本人可看到"编辑"和"删除"按钮 | P1 | 权限 | ✅ 通过 | `isAuthor || isAdmin` 时显示 Dropdown 菜单 |

---

## 三、帖子创作模块

### 3.1 发帖页 (NewPost)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| NEW-001 | 空标题提交 → 提示"请输入标题" | P0 | 表单校验 | ✅ 通过 | `{required:true, message:'请输入标题'}` |
| NEW-002 | 标题少于2字符 → 提示"标题至少2个字符" | P0 | 表单校验 | ✅ 通过 | `{min:2, message:'标题至少2个字符'}` |
| NEW-003 | 标题超过100字符 → 提示"标题最多100个字符" | P1 | 表单校验 | ✅ 通过 | `{max:100, message:'标题最多100个字符'}` |
| NEW-004 | 未选板块提交 → 提示"请选择板块" | P0 | 表单校验 | ✅ 通过 | `{required:true, message:'请选择板块'}` |
| NEW-005 | 空内容提交 → 提示"请输入帖子内容" | P0 | 表单校验 | ✅ 通过 | `if(!content.trim()) {message.warning('请输入帖子内容'); return}` |
| NEW-006 | AI 开关切换 → aiRequested 状态正确切换 | P1 | 交互 | ✅ 通过 | `<Switch checked={aiRequested} onChange={setAiRequested} />` |
| NEW-007 | 发布成功 → 提示"发布成功"并跳转 `/post/{id}` | P0 | 正向流程 | ✅ 通过 | `message.success('发布成功')` + `navigate(/post/${data.data})` |
| NEW-008 | 发布失败 → 提示"发布失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('发布失败')` |
| NEW-009 | 取消按钮 → 返回上一页 | P1 | 导航 | ✅ 通过 | `onClick={() => navigate(-1)}` |
| NEW-010 | 返回按钮 → 返回上一页 | P1 | 导航 | ✅ 通过 | `onClick={() => navigate(-1)}` |
| NEW-011 | 未登录访问 → AuthGuard 拦截跳转登录页 | P0 | 权限 | ✅ 通过 | 路由配置 `<AuthGuard><NewPost /></AuthGuard>` |

### 3.2 编辑帖子页 (EditPost)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| EDIT-001 | 页面加载 → 请求 `/posts/{id}` 填充表单 | P0 | 数据加载 | ✅ 通过 | `fetchPost()` → `form.setFieldsValue()` + `setContent()` |
| EDIT-002 | 帖子加载中 → 显示 Spin | P1 | 加载状态 | ✅ 通过 | `fetching ? <Spin size="large" />` |
| EDIT-003 | 帖子加载失败 → 提示"获取帖子失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('获取帖子失败')` |
| EDIT-004 | 修改标题/板块/内容后保存 → 请求 `PUT /posts/{id}` | P0 | 正向流程 | ✅ 通过 | `request.put(/posts/${id}, {...})` |
| EDIT-005 | 保存成功 → 提示"更新成功"并跳转 `/post/{id}` | P0 | 正向流程 | ✅ 通过 | `message.success('更新成功')` + `navigate(/post/${id})` |
| EDIT-006 | 保存失败 → 提示"更新失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('更新失败')` |
| EDIT-007 | 空内容保存 → 提示"请输入帖子内容" | P0 | 表单校验 | ✅ 通过 | `if(!content.trim()) {message.warning('请输入帖子内容'); return}` |

### 3.3 分类级联选择器 (CategoryCascader)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| CASC-001 | 加载分类树 → 渲染级联选项 | P0 | 数据加载 | ✅ 通过 | `request.get('/categories/tree')` → `transformToOptions()` |
| CASC-002 | 选择子分类 → onChange 返回子分类 ID | P0 | 交互 | ✅ 通过 | `handleChange` 取 `selectedValue[selectedValue.length-1]` |
| CASC-003 | 选择父分类 → onChange 返回父分类 ID | P1 | 交互 | ✅ 通过 | 同上逻辑，`changeOnSelect` 允许选择任意层级 |
| CASC-004 | `changeOnSelect` 允许选择任意层级 | P1 | 交互 | ✅ 通过 | `<Cascader changeOnSelect />` |
| CASC-005 | 传入无效 value → 不选中任何项 | P2 | 容错 | ✅ 通过 | `allIds.includes(value)` 校验，无效时 `cascaderValue=undefined` |

### 3.4 富文本编辑器 (RichEditor)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| RICH-001 | 工具栏按钮插入 Markdown 语法（粗体、斜体、删除线等） | P0 | 交互 | ✅ 通过 | `insertMarkdown('**','**')` 等9个工具栏按钮 |
| RICH-002 | 选中文本后点击工具栏 → 包裹选中文本 | P1 | 交互 | ✅ 通过 | `textarea.selectionStart/End` 获取选中文本并包裹 |
| RICH-003 | 编辑/预览模式切换 | P0 | 模式切换 | ✅ 通过 | `preview` state + 按钮 `onClick={() => setPreview(!preview)}` |
| RICH-004 | 预览模式正确渲染 Markdown（标题、列表、代码、粗体等） | P1 | 渲染 | ⚠️ 部分通过 | 简易渲染（标题/列表/粗体/斜体/行内代码），不支持表格、代码块、链接等完整 Markdown |
| RICH-005 | 空内容预览 → 显示"暂无内容" | P2 | 空状态 | ✅ 通过 | `value ? renderPreview() : '暂无内容'` |
| RICH-006 | TextArea 可调整高度 | P2 | 交互 | ✅ 通过 | `<Input.TextArea style={{resize:'vertical'}}>` |

---

## 四、评论模块

### 4.1 评论列表 (CommentList)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| CMT-001 | 加载评论列表 → 请求 `/comments/post/{postId}` | P0 | 数据加载 | ✅ 通过 | `request.get(/comments/post/${postId})` |
| CMT-002 | 评论数显示正确 | P1 | 数据展示 | ✅ 通过 | `Divider orientation="left">评论 ({commentCount})` |
| CMT-003 | 空评论 → 显示"暂无评论，快来抢沙发" | P1 | 空状态 | ✅ 通过 | `<Empty description="暂无评论，快来抢沙发" />` |
| CMT-004 | 分页切换 → 请求对应页评论 | P0 | 分页 | ✅ 通过 | `Pagination onChange={(p) => setPage(p)}` |
| CMT-005 | 点击回复 → 显示回复提示条，评论输入框切换为回复模式 | P1 | 交互 | ✅ 通过 | `handleReply` → `setReplyTo(comment)` + 回复提示条 |
| CMT-006 | 取消回复 → 隐藏回复提示条 | P1 | 交互 | ✅ 通过 | `Button onClick={() => setReplyTo(null)}` |
| CMT-007 | 评论成功 → 评论数 +1，列表刷新 | P0 | 正向流程 | ✅ 通过 | `handleCommentSuccess` → `setTotal(total+1)` + `onCommentCountChange(commentCount+1)` |
| CMT-008 | 删除评论 → 评论数 -1，列表刷新 | P1 | 交互 | ✅ 通过 | `handleDelete` → `setComments(filter)` + `onCommentCountChange(commentCount-1)` |

### 4.2 评论输入 (CommentInput)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| CINPUT-001 | 未登录 → 显示"请先登录后参与评论" | P0 | 权限 | ✅ 通过 | `if(!currentUser) return <div>请先登录后参与评论</div>` |
| CINPUT-002 | 空内容提交 → 提示"请输入评论内容" | P0 | 表单校验 | ✅ 通过 | `if(!content.trim()) {message.warning('请输入评论内容'); return}` |
| CINPUT-003 | 评论成功 → 清空输入框，回调 onSuccess | P0 | 正向流程 | ✅ 通过 | `setContent('')` + `onSuccess?.(optimisticComment)` |
| CINPUT-004 | 评论失败 → 提示"评论失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('评论失败')` |
| CINPUT-005 | 回复模式 placeholder 显示"回复 xxx..." | P1 | UI | ✅ 通过 | `replyToUser ? '回复 ${replyToUser}...' : '写下你的评论...'` |
| CINPUT-006 | 提交按钮在内容为空时 disabled | P1 | 交互 | ✅ 通过 | `disabled={!content.trim()}` |
| CINPUT-007 | 提交中 loading 状态 | P1 | 交互 | ✅ 通过 | `loading={loading}` |

---

## 五、交互模块

### 5.1 点赞按钮 (LikeButton)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| LIKE-001 | 未点赞状态点击 → 请求 `POST /interaction/like`，状态变为已点赞，计数+1 | P0 | 正向流程 | ✅ 通过 | `request.post('/interaction/like')` + `onLikeChange(!liked, newCount)` |
| LIKE-002 | 已点赞状态点击 → 取消点赞，计数-1 | P0 | 正向流程 | ✅ 通过 | 同一接口，`newLiked=!liked`，`newCount=liked?count-1:count+1` |
| LIKE-003 | 操作失败 → 提示"操作失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('操作失败')` |
| LIKE-004 | 点击中 loading 状态，防止重复点击 | P1 | 交互 | ✅ 通过 | `loading={loading}` + `setLoading(true/false)` |
| LIKE-005 | 不同 size（small/middle/large）正确渲染 | P2 | UI | ✅ 通过 | `size={size}` prop 传入 Button |
| LIKE-006 | text=false 时不显示计数 | P2 | UI | ✅ 通过 | `{text && likeCount}` 条件渲染 |

### 5.2 收藏按钮 (FavoriteButton)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| FAV-001 | 未收藏状态点击 → 请求 `POST /interaction/favorite`，状态变为已收藏，计数+1 | P0 | 正向流程 | ✅ 通过 | `request.post('/interaction/favorite', null, {params:{postId}})` |
| FAV-002 | 已收藏状态点击 → 取消收藏，计数-1 | P0 | 正向流程 | ✅ 通过 | 同一接口，`newFavorited=!favorited` |
| FAV-003 | 操作失败 → 提示"操作失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('操作失败')` |
| FAV-004 | 已收藏状态按钮颜色变为金色 | P1 | UI | ✅ 通过 | `style={favorited ? {color:'#faad14', borderColor:'#faad14'} : undefined}` |

### 5.3 分享按钮 (ShareButton)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| SHARE-001 | 点击复制链接 → 写入剪贴板，提示"链接已复制到剪贴板" | P0 | 正向流程 | ✅ 通过 | `navigator.clipboard.writeText()` + `message.success('链接已复制到剪贴板')` |
| SHARE-002 | 微信分享 → 弹出 Modal 显示二维码 | P1 | 交互 | ✅ 通过 | `setModalOpen(true)` + `<Modal title="微信分享">` |
| SHARE-003 | 链接分享 → 复制链接并记录分享 | P1 | 交互 | ✅ 通过 | `handleLink()` → `handleCopyLink()` + `recordShare(ShareChannel.LINK)` |
| SHARE-004 | 分享记录 → 请求 `POST /interaction/share` 携带 channel 参数 | P1 | 数据上报 | ✅ 通过 | `request.post('/interaction/share', null, {params:{postId, channel}})` |
| SHARE-005 | 剪贴板 API 不可用时不崩溃 | P2 | 容错 | ❌ 未通过 | 未做 try-catch 包裹 `navigator.clipboard.writeText()`，在不支持 Clipboard API 的浏览器中会抛异常 |

---

## 六、通知模块

### 6.1 通知铃铛 (NotificationBell)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| BELL-001 | 页面加载 → 请求未读数，每30秒轮询 | P0 | 数据加载 | ✅ 通过 | `fetchUnreadCount()` + `setInterval(fetchUnreadCount, 30000)` |
| BELL-002 | 未读数 > 0 → Badge 显示数字 | P0 | UI | ✅ 通过 | `<Badge count={unreadCount} size="small">` |
| BELL-003 | 点击铃铛 → 弹出 Popover，请求最近10条通知 | P0 | 交互 | ✅ 通过 | `onOpenChange` → `fetchNotifications(1, 10)` |
| BELL-004 | 点击未读通知 → 标记已读，跳转关联帖子 | P0 | 正向流程 | ✅ 通过 | `markAsRead(item.id)` + `navigate(/post/${item.relatedId})` |
| BELL-005 | "全部已读"按钮 → 标记所有通知已读 | P1 | 交互 | ✅ 通过 | `Button onClick={handleMarkAll}` → `markAllAsRead()` |
| BELL-006 | "查看全部通知" → 跳转 `/notifications` | P1 | 导航 | ✅ 通过 | `Button onClick={() => navigate('/notifications')}` |
| BELL-007 | 空通知 → 显示"暂无通知" | P2 | 空状态 | ✅ 通过 | `<Empty description="暂无通知" />` |
| BELL-008 | 不同通知类型图标和颜色区分（评论/点赞/收藏/系统/AI/举报） | P1 | UI | ✅ 通过 | `typeAccentMap` 定义6种类型的不同 icon/accent/bg |

### 6.2 通知页面 (Notifications)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| NOTIF-001 | 页面加载 → 请求通知列表 | P0 | 数据加载 | ✅ 通过 | `useEffect` → `fetchNotifications(1, 50)` |
| NOTIF-002 | 全部/未读 Tab 切换 → 过滤通知列表 | P0 | 筛选 | ✅ 通过 | `activeTab==='unread' ? notifications.filter(n=>n.isRead===0) : notifications` |
| NOTIF-003 | 点击未读通知 → 标记已读，跳转关联帖子 | P0 | 正向流程 | ✅ 通过 | `markAsRead(item.id)` + `navigate(/post/${item.relatedId})` |
| NOTIF-004 | "全部标记已读" → 批量标记 | P1 | 交互 | ✅ 通过 | `Button onClick={handleMarkAll}` → `markAllAsRead()` |
| NOTIF-005 | 未读通知左侧彩色边框和背景高亮 | P1 | UI | ✅ 通过 | `borderLeft: isUnread ? '3px solid ${config.accent}'` + `background: isUnread ? config.bg` |
| NOTIF-006 | 空通知列表 → 显示"暂无通知"或"没有未读通知" | P1 | 空状态 | ✅ 通过 | `activeTab==='unread' ? '没有未读通知' : '暂无通知'` |
| NOTIF-007 | 通知时间显示相对时间（如"3分钟前"） | P2 | UI | ✅ 通过 | `dayjs(item.createdAt).fromNow()` |

### 6.3 通知 Store (useNotificationStore)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| NSTORE-001 | `fetchUnreadCount` → 更新 unreadCount | P0 | 状态管理 | ✅ 通过 | `set({unreadCount: data.data})` |
| NSTORE-002 | `fetchNotifications` → 更新 notifications 和 loading | P0 | 状态管理 | ✅ 通过 | `set({loading:true})` → `set({notifications: data.data.list})` → `set({loading:false})` |
| NSTORE-003 | `markAsRead` → 通知 isRead 变为1，unreadCount-1（最小为0） | P0 | 状态管理 | ✅ 通过 | `notifications.map(n=>n.id===id ? {...n, isRead:1} : n)` + `Math.max(0, unreadCount-1)` |
| NSTORE-004 | `markAllAsRead` → 所有通知 isRead 变为1，unreadCount=0 | P0 | 状态管理 | ✅ 通过 | `notifications.map(n=>({...n, isRead:1}))` + `unreadCount:0` |
| NSTORE-005 | `incrementUnread` → unreadCount+1 | P1 | 状态管理 | ✅ 通过 | `set((state) => ({unreadCount: state.unreadCount+1}))` |

### 6.4 WebSocket (useWebSocket)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| WS-001 | 已登录用户 → 自动连接 WebSocket `/ws/notifications` | P0 | 连接 | ✅ 通过 | MainLayout 中 `useWebSocket({enabled:!!user})`，`enabled` 时 `connect()` |
| WS-002 | 未登录用户 → 不连接 | P0 | 条件连接 | ✅ 通过 | `enabled=false` 时 `connect()` 中 `if(!enabled) return` |
| WS-003 | 收到 NOTIFICATION 类型消息 → incrementUnread | P0 | 消息处理 | ✅ 通过 | `case 'NOTIFICATION': incrementUnread()` |
| WS-004 | 收到 UNREAD_COUNT 类型消息 → fetchUnreadCount | P0 | 消息处理 | ✅ 通过 | `case 'UNREAD_COUNT': fetchUnreadCount()` |
| WS-005 | 连接断开 → 自动重连（最多 maxRetries 次） | P1 | 重连机制 | ✅ 通过 | `ws.onclose` → `if(retriesRef.current < maxRetries) setTimeout(connect, reconnectInterval)` |
| WS-006 | 重连间隔 reconnectInterval 正确 | P2 | 重连机制 | ✅ 通过 | 默认 `reconnectInterval=5000`，`setTimeout(connect, reconnectInterval)` |
| WS-007 | 组件卸载 → 断开 WebSocket 连接 | P0 | 清理 | ✅ 通过 | `useEffect` 返回 `disconnect`，清除 timer 和关闭 ws |
| WS-008 | `send` 方法在连接打开时发送 JSON 数据 | P2 | 发送消息 | ✅ 通过 | `if(wsRef.current?.readyState===WebSocket.OPEN) wsRef.current.send(JSON.stringify(data))` |

---

## 七、用户中心模块

### 7.1 个人资料页 (Profile)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| PROF-001 | 访问 `/profile`（自己）→ 显示当前用户信息 | P0 | 数据加载 | ✅ 通过 | `isSelf` 时使用 `currentUser` 或 `fetchProfile()` |
| PROF-002 | 访问 `/profile/:id`（他人）→ 请求该用户信息 | P0 | 数据加载 | ✅ 通过 | `!isSelf && id` → `fetchUserProfile(Number(id))` |
| PROF-003 | 用户不存在 → 显示"用户不存在" | P1 | 异常流程 | ✅ 通过 | `!profileUser ? <Text>用户不存在</Text>` |
| PROF-004 | 自己的页面显示"编辑资料"和"修改密码"按钮 | P0 | 权限 | ✅ 通过 | `{isSelf && <Button>编辑资料</Button> && <Button>修改密码</Button>}` |
| PROF-005 | 他人的页面不显示编辑按钮 | P0 | 权限 | ✅ 通过 | `isSelf` 条件控制 |
| PROF-006 | 头像上传（仅自己）→ 见 ImageUpload 测试 | P1 | 交互 | ✅ 通过 | `{isSelf ? <AvatarUpload /> : <Avatar />}` |
| PROF-007 | 用户状态标签（正常/禁用/禁言）正确显示 | P1 | UI | ✅ 通过 | `statusMap` 映射3种状态到 label+color |
| PROF-008 | 角色标签（管理员/版主/用户）正确显示 | P1 | UI | ✅ 通过 | `roleLabelMap` 映射 RoleCode 到中文标签 |
| PROF-009 | 详细信息（邮箱、手机、注册时间等）正确显示 | P1 | 数据展示 | ✅ 通过 | `<Descriptions>` 展示 username/nickname/email/phone/createdAt |
| PROF-010 | 未设置邮箱/手机 → 显示"未设置" | P2 | 空状态 | ✅ 通过 | `{profileUser.email || <Text type="secondary">未设置</Text>}` |

### 7.2 编辑资料弹窗
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| EDITPROF-001 | 昵称为空提交 → 提示"请输入昵称" | P0 | 表单校验 | ✅ 通过 | `{required:true, message:'请输入昵称'}` |
| EDITPROF-002 | 昵称超过50字符 → 提示"昵称最多50个字符" | P1 | 表单校验 | ✅ 通过 | `{max:50, message:'昵称最多50个字符'}` |
| EDITPROF-003 | 无效邮箱格式 → 提示"请输入有效的邮箱地址" | P1 | 表单校验 | ✅ 通过 | `{type:'email', message:'请输入有效的邮箱地址'}` |
| EDITPROF-004 | 无效手机号 → 提示"请输入有效的手机号" | P1 | 表单校验 | ✅ 通过 | `{pattern:/^1[3-9]\d{9}$/, message:'请输入有效的手机号'}` |
| EDITPROF-005 | 简介超过200字符 → 提示"简介最多200个字符" | P2 | 表单校验 | ✅ 通过 | `{max:200, message:'简介最多200个字符'}` |
| EDITPROF-006 | 保存成功 → 更新 store 中的用户信息，提示"更新成功" | P0 | 正向流程 | ✅ 通过 | `setUser(data.data)` + `message.success('更新成功')` |
| EDITPROF-007 | 保存失败 → 本地更新并提示"更新成功（本地）" | P1 | 降级处理 | ✅ 通过 | catch 块 `setUser(updatedUser)` + `message.success('更新成功（本地）')` |
| EDITPROF-008 | 取消 → 关闭弹窗，重置表单 | P1 | 交互 | ✅ 通过 | `onCancel={() => setEditModalOpen(false)}`，`destroyOnClose` |

### 7.3 修改密码弹窗 (ChangePasswordModal)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| PWD-001 | 空当前密码提交 → 提示"请输入当前密码" | P0 | 表单校验 | ✅ 通过 | `{required:true, message:'请输入当前密码'}` |
| PWD-002 | 新密码少于6字符 → 提示"密码至少6个字符" | P0 | 表单校验 | ✅ 通过 | `{min:6, message:'密码至少6个字符'}` |
| PWD-003 | 确认新密码不一致 → 提示"两次输入的密码不一致" | P0 | 表单校验 | ✅ 通过 | 自定义 validator 比对 `getFieldValue('newPassword')` |
| PWD-004 | 修改成功 → 提示"密码修改成功，请重新登录"，自动登出并跳转登录页 | P0 | 正向流程 | ✅ 通过 | `message.success('密码修改成功，请重新登录')` + `logout()` + `window.location.href='/login'` |
| PWD-005 | 修改失败 → 提示"该功能暂未开放，请联系管理员修改密码" | P1 | 异常流程 | ✅ 通过 | catch 块 `message.warning('该功能暂未开放，请联系管理员修改密码')` |
| PWD-006 | 取消 → 关闭弹窗，重置表单 | P1 | 交互 | ✅ 通过 | `handleCancel` → `form.resetFields()` + `onClose()` |

### 7.4 用户内容标签页 (UserContentTabs)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| UTAB-001 | 帖子 Tab → 请求用户帖子列表 | P0 | 数据加载 | ✅ 通过 | `fetchPosts(page)` → `request.get('/posts', {params:{authorId:userId}})` |
| UTAB-002 | 评论 Tab → 请求用户评论列表 | P0 | 数据加载 | ✅ 通过 | `fetchComments(page)` → `request.get('/user/comments')` |
| UTAB-003 | 收藏 Tab → 请求用户收藏列表 | P0 | 数据加载 | ✅ 通过 | `fetchFavorites(page)` → `request.get('/user/favorites')` |
| UTAB-004 | Tab 切换 → 加载对应数据 | P0 | 交互 | ✅ 通过 | `handleTabChange` → `setActiveTab(key)` |
| UTAB-005 | 空列表 → 显示 Empty 组件 | P1 | 空状态 | ✅ 通过 | 各 Tab 均有 `<Empty description="暂无..." />` |
| UTAB-006 | 帖子/评论/收藏分页 | P1 | 分页 | ✅ 通过 | 各 Tab 均有 `<Pagination>` 组件 |
| UTAB-007 | 点击帖子 → 跳转帖子详情 | P1 | 导航 | ✅ 通过 | `onClick={() => navigate(/post/${item.id})}` |

### 7.5 头像上传 (AvatarUpload)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| AVATAR-001 | 上传非图片文件 → 提示"只能上传图片文件" | P0 | 文件校验 | ✅ 通过 | `if(!file.type.startsWith('image/')) {message.error('只能上传图片文件')}` |
| AVATAR-002 | 上传超过2MB图片 → 提示"图片大小不能超过 2MB" | P0 | 大小校验 | ✅ 通过 | `if(file.size/1024/1024 >= 2) {message.error('图片大小不能超过 2MB')}` |
| AVATAR-003 | 上传成功 → 回调 onUploadSuccess，提示"上传成功" | P0 | 正向流程 | ✅ 通过 | `onUploadSuccess?.(data.data.url)` + `message.success('上传成功')` |
| AVATAR-004 | 上传失败 → 提示"上传失败" | P0 | 异常流程 | ✅ 通过 | catch 块 `message.error('上传失败')` |
| AVATAR-005 | 上传中头像半透明 | P2 | UI | ✅ 通过 | `style={{opacity: uploading ? 0.5 : 1}}` |

### 7.6 图片上传 (ImageUpload)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| IMG-001 | 上传非图片文件 → 提示"只能上传图片文件" | P0 | 文件校验 | ✅ 通过 | `if(!file.type.startsWith('image/')) {message.error('只能上传图片文件')}` |
| IMG-002 | 上传超过5MB图片 → 提示"图片大小不能超过 5MB" | P0 | 大小校验 | ✅ 通过 | `if(file.size/1024/1024 >= 5) {message.error('图片大小不能超过 5MB')}` |
| IMG-003 | 上传成功 → 图片列表更新 | P0 | 正向流程 | ✅ 通过 | `onChange?.([...value, data.data.url])` |
| IMG-004 | 超过 maxCount → 隐藏上传按钮 | P1 | 限制 | ✅ 通过 | `{value.length < maxCount && <Upload>}` |
| IMG-005 | 点击图片 → 弹出预览 Modal | P1 | 交互 | ✅ 通过 | `onClick={() => handlePreview(url)}` → `Modal.info` |
| IMG-006 | 点击删除按钮 → 移除图片 | P1 | 交互 | ✅ 通过 | `onClick={() => handleRemove(index)}` → `onChange?.(filter)` |

---

## 八、搜索模块

### 8.1 搜索页 (Search)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| SRCH-001 | URL 参数 `?q=keyword` → 自动搜索 | P0 | URL 参数 | ✅ 通过 | `const keyword = searchParams.get('q') \|\| ''` |
| SRCH-002 | 输入关键词搜索 → 更新 URL 参数并请求 | P0 | 正向流程 | ✅ 通过 | `handleSearch` → `setSearchParams({q:v})` |
| SRCH-003 | 清空搜索 → 重置所有筛选 | P1 | 交互 | ✅ 通过 | `handleClear` → `setSearchParams({})` + 重置所有状态 |
| SRCH-004 | 排序切换 → 重新请求 | P0 | 排序 | ✅ 通过 | `handleSortChange` → `setSortBy` + `setPage(1)` |
| SRCH-005 | 分类筛选 → 重新请求 | P0 | 筛选 | ✅ 通过 | `handleCategoryChange` → `setCategoryId` + `setPage(1)` |
| SRCH-006 | 热门搜索关键词点击 → 自动填入并搜索 | P1 | 交互 | ✅ 通过 | `handleHotKeywordClick` → `setSearchParams({q:kw})` |
| SRCH-007 | 搜索结果为空 → 显示"未找到与xxx相关的帖子" | P1 | 空状态 | ✅ 通过 | `keyword ? '未找到与"${keyword}"相关的帖子' : '暂无帖子'` |
| SRCH-008 | 搜索结果统计"搜索 'xxx' 的结果，共 N 篇" | P1 | UI | ✅ 通过 | `搜索 "<Text strong>{keyword}</Text>" 的结果，共 {total} 篇` |
| SRCH-009 | 分页 | P0 | 分页 | ✅ 通过 | `<Pagination onChange={handlePageChange}>` |

### 8.2 头部搜索 (HeaderSearch)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| HSRCH-001 | 聚焦输入框（无输入）→ 显示热门搜索 | P1 | 交互 | ✅ 通过 | `onFocus={handleFocus}` → `setOptions(buildDefaultOptions())` |
| HSRCH-002 | 输入关键词 → 300ms 防抖请求搜索建议 | P0 | 搜索建议 | ✅ 通过 | `setTimeout(() => request.get('/posts', ...), 300)` |
| HSRCH-003 | 选择建议项 → 跳转搜索页 | P0 | 导航 | ✅ 通过 | `onSelect` → `navigate(/search?q=${encodeURIComponent(selected)})` |
| HSRCH-004 | 回车搜索 → 跳转搜索页 | P0 | 导航 | ✅ 通过 | `onPressEnter={() => handleSearch(value)}` |
| HSRCH-005 | 清空输入 → 清除建议 | P1 | 交互 | ✅ 通过 | AutoComplete `allowClear` + `onSearch` 中空值处理 |

---

## 九、AI 回答模块

### 9.1 AI 回答卡片 (AIReplyCard)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| AI-001 | aiRequested=true → 请求 `/ai/posts/{postId}/response` | P0 | 数据加载 | ✅ 通过 | `useEffect` → `if(aiRequested && postId) fetchAiResponse()` |
| AI-002 | aiRequested=false → 不渲染 | P0 | 条件渲染 | ✅ 通过 | `if(!aiRequested) return null` |
| AI-003 | 生成中状态 → 显示 Skeleton + "生成中"标签 | P1 | 加载状态 | ✅ 通过 | `loading && !fetched` → `<Skeleton active />` + `<Tag color="processing">生成中</Tag>` |
| AI-004 | 已完成状态 → 显示打字机效果内容 | P0 | UI | ✅ 通过 | `isCompleted && showTypewriter` → `<TypewriterText content={aiResponse.content} speed={20} />` |
| AI-005 | 失败状态 → 显示错误信息 | P1 | 异常流程 | ✅ 通过 | `isFailed` → 红色背景显示 `aiResponse.errorMessage` |
| AI-006 | "重新生成"按钮 → 请求 `/ai/posts/{postId}/retry` | P1 | 交互 | ✅ 通过 | `handleRegenerate` → `request.post(/ai/posts/${postId}/retry)` |
| AI-007 | 重新生成成功 → 内容重新以打字机效果展示 | P1 | 交互 | ✅ 通过 | `setShowTypewriter(true)` + `setAiResponse(data.data)` |
| AI-008 | 重新生成失败 → 提示"重新生成失败" | P1 | 异常流程 | ✅ 通过 | catch 块 `message.error('重新生成失败')` |
| AI-009 | Token 用量显示（promptTokens/completionTokens） | P2 | UI | ✅ 通过 | `aiResponse.promptTokens && <Text>tokens: {promptTokens}/{completionTokens}</Text>` |
| AI-010 | 打字机效果完成后显示完整 Markdown 渲染 | P2 | UI | ✅ 通过 | TypewriterText 完成后 `setDisplayed(content)`，完整内容渲染 |
| AI-011 | 模型名称显示 | P2 | UI | ✅ 通过 | `aiResponse.model && <Text>{aiResponse.model}</Text>` |

---

## 十、布局与导航模块

### 10.1 主布局 (MainLayout)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| LAYOUT-001 | 侧边栏折叠/展开 → collapsed 状态切换 | P1 | 交互 | ✅ 通过 | `useState(false)` + `Sidebar collapsed={collapsed}` + `Header onCollapse={setCollapsed}` |
| LAYOUT-002 | WebSocket 在已登录时自动连接 | P0 | 功能 | ✅ 通过 | `useWebSocket({enabled:!!user})` |
| LAYOUT-003 | WebSocket 在未登录时不连接 | P0 | 功能 | ✅ 通过 | `enabled=!!user`，未登录时 `enabled=false` |

### 10.2 头部导航 (Header)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| HDR-001 | 折叠按钮切换侧边栏 | P1 | 交互 | ✅ 通过 | `onClick={() => onCollapse(!collapsed)}` |
| HDR-002 | 点击 Logo → 跳转首页 | P1 | 导航 | ✅ 通过 | `onClick={() => navigate('/')}` |
| HDR-003 | 已登录 → 显示用户头像和昵称下拉菜单 | P0 | UI | ✅ 通过 | `{user ? <Dropdown menu={userMenuItems}><Avatar/><Text/></Dropdown> : ...}` |
| HDR-004 | 未登录 → 显示"登录"链接 | P0 | UI | ✅ 通过 | `!user && <Text onClick={() => navigate(/login?redirect=...)>登录</Text>` |
| HDR-005 | "个人中心"菜单 → 跳转 `/profile` | P1 | 导航 | ✅ 通过 | `onClick: () => navigate('/profile')` |
| HDR-006 | "退出登录"菜单 → 登出并跳转 `/login` | P0 | 正向流程 | ✅ 通过 | `onClick: () => {logout(); navigate('/login')}` |
| HDR-007 | 通知铃铛显示（见 NotificationBell 测试） | P0 | 功能 | ✅ 通过 | `<NotificationBell />` 组件已渲染 |

### 10.3 侧边栏 (Sidebar)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| SIDE-001 | 菜单项点击 → 路由跳转 | P0 | 导航 | ✅ 通过 | `handleMenuClick` → `navigate(key)` |
| SIDE-002 | 当前路由高亮对应菜单项 | P1 | UI | ✅ 通过 | `selectedKeys={[location.pathname]}` |
| SIDE-003 | 板块子菜单动态加载分类树 | P0 | 数据加载 | ✅ 通过 | `request.get('/categories/tree')` → `categories.map()` |
| SIDE-004 | 折叠状态 → 仅显示图标 | P1 | UI | ✅ 通过 | `<Sider collapsed={collapsed} collapsedWidth={64} />` |
| SIDE-005 | "发帖"菜单 → 跳转 `/new-post`（需登录） | P0 | 导航 | ✅ 通过 | 菜单项 `key='/new-post'`，路由配置 `AuthGuard` |

### 10.4 管理后台布局 (AdminLayout)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| ADMIN-L-001 | 侧边栏菜单项点击 → 路由跳转 | P0 | 导航 | ✅ 通过 | `handleMenuClick` → `navigate(key)` |
| ADMIN-L-002 | 当前路由高亮 | P1 | UI | ✅ 通过 | `selectedKeys={[location.pathname]}` |
| ADMIN-L-003 | "返回前台"链接 → 跳转 `/` | P1 | 导航 | ✅ 通过 | `onClick={() => navigate('/')}` |
| ADMIN-L-004 | 侧边栏折叠/展开 | P1 | 交互 | ✅ 通过 | `<Sider collapsible collapsed={collapsed} onCollapse={setCollapsed} />` |

---

## 十一、管理后台模块

> 注：当前管理后台各页面（Dashboard、UserManage、CategoryManage、PostManage、ReportManage、SystemConfig）均为占位页面，仅显示文字。以下为功能实现后的预期测试项。

| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| ADM-001 | 非管理员访问 `/admin/*` → 被拦截跳转首页 | P0 | 权限 | ✅ 通过 | AdminGuard: `!user?.roles.includes(RoleCode.ADMIN)` → `Navigate to="/"` |
| ADM-002 | 管理员访问 `/admin` → 渲染 Dashboard | P0 | 路由 | ✅ 通过 | 路由配置 `<Route path="/admin" element={<AdminDashboard />} />`，占位页面已渲染 |
| ADM-003 | 管理员访问 `/admin/users` → 渲染用户管理 | P0 | 路由 | ✅ 通过 | 路由配置 `<Route path="/admin/users" element={<UserManage />} />`，占位页面已渲染 |
| ADM-004 | 管理员访问 `/admin/categories` → 渲染板块管理 | P0 | 路由 | ✅ 通过 | 路由配置 `<Route path="/admin/categories" element={<CategoryManage />} />` |
| ADM-005 | 管理员访问 `/admin/posts` → 渲染帖子管理 | P0 | 路由 | ✅ 通过 | 路由配置 `<Route path="/admin/posts" element={<PostManage />} />` |
| ADM-006 | 管理员访问 `/admin/reports` → 渲染举报管理 | P0 | 路由 | ✅ 通过 | 路由配置 `<Route path="/admin/reports" element={<ReportManage />} />` |
| ADM-007 | 管理员访问 `/admin/config` → 渲染系统配置 | P0 | 路由 | ✅ 通过 | 路由配置 `<Route path="/admin/config" element={<SystemConfig />} />` |

---

## 十二、API 层与全局错误处理

| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| API-001 | 请求成功 (code=200) → 正常返回数据 | P0 | 正向流程 | ✅ 通过 | 响应拦截器 `res.code===200` 时正常返回 |
| API-002 | 业务错误 (code≠200) → message.error 提示错误信息 | P0 | 异常流程 | ✅ 通过 | `if(res.code!==200) {message.error(errorMsg); return Promise.reject()}` |
| API-003 | 401 响应 → 触发 token 刷新或跳转登录 | P0 | 认证 | ✅ 通过 | HTTP 401 → refreshToken 刷新 → 失败则 `handleUnauthorized()` |
| API-004 | 400 响应 → 提示"请求参数错误" | P1 | 异常流程 | ✅ 通过 | `errorMessages[400] = '请求参数错误'` |
| API-005 | 403 响应 → 提示"没有访问权限" | P1 | 异常流程 | ✅ 通过 | `errorMessages[403] = '没有访问权限'` |
| API-006 | 404 响应 → 提示"请求资源不存在" | P1 | 异常流程 | ✅ 通过 | `errorMessages[404] = '请求资源不存在'` |
| API-007 | 500 响应 → 提示"服务器内部错误" | P0 | 异常流程 | ✅ 通过 | `errorMessages[500] = '服务器内部错误'` |
| API-008 | 网络断开 → 提示"请求失败 (网络错误)" | P1 | 异常流程 | ✅ 通过 | `errorMessages[status \|\| 0] \|\| '请求失败 (网络错误)'` |
| API-009 | 请求超时 (15s) → 提示超时错误 | P1 | 异常流程 | ✅ 通过 | `timeout: 15000`，超时触发 AxiosError |
| API-010 | 并发 401 时 token 刷新仅触发一次，其他请求排队 | P1 | 并发处理 | ✅ 通过 | `isRefreshing` + `pendingRequests` 队列机制 |

---

## 十三、状态管理 (Zustand Stores)

### 13.1 认证 Store (useAuthStore)
| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| ASTORE-001 | `login` 成功 → 设置 token、user、isAuthenticated=true | P0 | 状态管理 | ✅ 通过 | `setTokens(token, refreshToken, expiresIn)` + `set({user, isAuthenticated:true})` |
| ASTORE-002 | `login` 失败 → 不改变状态 | P0 | 异常流程 | ✅ 通过 | login 方法无 catch 处理，异常由调用方处理，状态不变 |
| ASTORE-003 | `logout` → 清除 token，user=null，isAuthenticated=false | P0 | 状态管理 | ✅ 通过 | `clearTokens()` + `set({user:null, isAuthenticated:false})` |
| ASTORE-004 | `fetchProfile` 成功 → 更新 user | P0 | 状态管理 | ✅ 通过 | `set({user:data.data, isAuthenticated:true})` |
| ASTORE-005 | `setUser` → 更新 user 和 isAuthenticated | P1 | 状态管理 | ✅ 通过 | `set({user, isAuthenticated:true})` |

---

## 十四、响应式与兼容性

| 编号 | 测试项 | 优先级 | 测试类型 | 验证结果 | 说明 |
|------|--------|--------|----------|----------|------|
| RESP-001 | 1920px 宽屏 → 布局正常，内容居中 | P1 | 响应式 | ⏳ 待运行验证 | 需实际浏览器测试，代码使用 `maxWidth:1200` + `margin:'0 auto'` |
| RESP-002 | 1366px 标准屏 → 布局正常 | P1 | 响应式 | ⏳ 待运行验证 | 需实际浏览器测试 |
| RESP-003 | 768px 平板 → 侧边栏折叠，内容自适应 | P2 | 响应式 | ⏳ 待运行验证 | 侧边栏 `collapsedWidth={64}`，但无响应式断点自动折叠 |
| RESP-004 | 375px 手机 → 移动端适配 | P2 | 响应式 | ⏳ 待运行验证 | 无移动端适配代码，可能布局异常 |
| COMPAT-001 | Chrome 最新版 → 功能正常 | P1 | 兼容性 | ⏳ 待运行验证 | 需实际测试 |
| COMPAT-002 | Firefox 最新版 → 功能正常 | P2 | 兼容性 | ⏳ 待运行验证 | 需实际测试 |
| COMPAT-003 | Safari 最新版 → 功能正常 | P2 | 兼容性 | ⏳ 待运行验证 | 需实际测试，注意 Clipboard API 兼容性 |
| COMPAT-004 | Edge 最新版 → 功能正常 | P2 | 兼容性 | ⏳ 待运行验证 | 需实际测试 |

---

## 测试统计

| 模块 | P0 | P1 | P2 | 合计 | 通过 | 部分通过 | 未通过 | 待验证 |
|------|-----|-----|-----|------|------|----------|--------|--------|
| 认证与授权 | 18 | 6 | 1 | 25 | 25 | 0 | 0 | 0 |
| 帖子浏览 | 15 | 14 | 5 | 34 | 33 | 1 | 0 | 0 |
| 帖子创作 | 12 | 7 | 4 | 23 | 22 | 1 | 0 | 0 |
| 评论模块 | 7 | 6 | 0 | 13 | 13 | 0 | 0 | 0 |
| 交互模块 | 7 | 5 | 2 | 14 | 13 | 0 | 1 | 0 |
| 通知模块 | 12 | 7 | 4 | 23 | 23 | 0 | 0 | 0 |
| 用户中心 | 13 | 13 | 4 | 30 | 30 | 0 | 0 | 0 |
| 搜索模块 | 7 | 5 | 0 | 12 | 12 | 0 | 0 | 0 |
| AI 回答 | 3 | 4 | 4 | 11 | 11 | 0 | 0 | 0 |
| 布局导航 | 6 | 8 | 0 | 14 | 14 | 0 | 0 | 0 |
| 管理后台 | 7 | 0 | 0 | 7 | 7 | 0 | 0 | 0 |
| API 与错误处理 | 4 | 6 | 0 | 10 | 10 | 0 | 0 | 0 |
| 状态管理 | 4 | 1 | 0 | 5 | 5 | 0 | 0 | 0 |
| 响应式与兼容性 | 0 | 3 | 7 | 10 | 0 | 0 | 0 | 10 |
| **合计** | **115** | **85** | **31** | **231** | **218** | **2** | **1** | **10** |

---

## 验证结果汇总

### ✅ 通过：218 项（94.4%）
### ⚠️ 部分通过：2 项（0.9%）
### ❌ 未通过：1 项（0.4%）
### ⏳ 待运行验证：10 项（4.3%）

---

## 问题清单

### ❌ 未通过项

| 编号 | 测试项 | 问题描述 | 建议修复 |
|------|--------|----------|----------|
| SHARE-005 | 剪贴板 API 不可用时不崩溃 | `navigator.clipboard.writeText()` 未做 try-catch，在不支持 Clipboard API 的环境（如 HTTP 页面、旧浏览器）中会抛出未捕获异常 | 在 `handleCopyLink` 中包裹 try-catch，降级使用 `document.execCommand('copy')` 或提示用户手动复制 |

### ⚠️ 部分通过项

| 编号 | 测试项 | 问题描述 | 建议修复 |
|------|--------|----------|----------|
| POST-006 | 帖子内容正确渲染（Markdown） | 当前使用简易字符串解析，仅支持 `#` 标题、`-` 列表、行内代码、粗体/斜体，不支持代码块（```）、表格、链接、图片等完整 Markdown 语法 | 引入成熟 Markdown 渲染库（如 react-markdown + remark-gfm） |
| RICH-004 | 预览模式正确渲染 Markdown | 同 POST-006，预览模式使用相同的简易渲染器 | 同上，统一使用 Markdown 渲染库 |

### ⏳ 待运行验证项

| 编号 | 说明 |
|------|------|
| RESP-001 ~ RESP-004 | 响应式布局需在浏览器中实际测试，代码层面无响应式断点自动折叠侧边栏，移动端可能布局异常 |
| COMPAT-001 ~ COMPAT-004 | 浏览器兼容性需实际测试，特别注意 Safari 的 Clipboard API 限制 |

---

**说明：**
- **P0**：核心功能，必须通过，阻塞发布
- **P1**：重要功能，影响用户体验，应尽快修复
- **P2**：次要功能/边界场景，可延后处理
- 管理后台页面当前为占位实现，测试项为功能完善后的预期项
- 建议优先执行 P0 级别的 115 项测试，覆盖认证流程、核心 CRUD、交互操作和 API 错误处理