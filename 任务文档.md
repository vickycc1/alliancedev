# 企业级技术交流分享社区系统 — 任务文档

---

## 一、项目概述

### 1.1 项目名称
TechCommunity — 企业级技术交流分享社区

### 1.2 项目目标
构建一个功能完整、交互清晰的企业级技术交流分享社区平台，满足日常社区运营与用户互动的核心业务场景，可稳定支撑一定数量用户同时在线和使用。系统核心亮点为 AI 智能回答功能，帖主发帖时可请求 AI 回答，AI 回答内容作为帖子一楼展示，所有用户可见。

### 1.3 技术栈

| 层级 | 技术选型 | 版本 |
|------|---------|------|
| 前端框架 | React | 18.x |
| 前端构建 | Vite | 5.x |
| 前端UI库 | Ant Design | 5.x |
| 前端路由 | React Router | v6 |
| 前端状态管理 | Zustand | 4.x |
| 前端HTTP | Axios | 1.x |
| 前端富文本 | React Quill | 2.x |
| 后端框架 | Spring Boot | 3.2.x |
| 后端安全 | Spring Security + JWT + CAS Client | - |
| 后端ORM | MyBatis-Plus | 3.5.x |
| 数据库 | MySQL | 8.0 |
| 缓存 | Redis | 7.x |
| AI接口 | OpenAI 兼容 API（可配置切换） | - |
| 实时通信 | WebSocket (STOMP) | - |

### 1.4 项目结构

```
alliancedev/
├── backend/                              # Spring Boot 后端
│   ├── pom.xml
│   └── src/main/java/com/techcommunity/
│       ├── TechCommunityApplication.java # 启动类
│       ├── config/                       # 配置类
│       │   ├── SecurityConfig.java       # Spring Security 配置
│       │   ├── CorsConfig.java           # 跨域配置
│       │   ├── RedisConfig.java          # Redis 配置
│       │   ├── WebSocketConfig.java      # WebSocket 配置
│       │   ├── CasConfig.java            # CAS 客户端配置
│       │   └── MybatisPlusConfig.java    # MyBatis-Plus 配置
│       ├── common/                       # 通用类
│       │   ├── Result.java               # 统一响应封装
│       │   ├── PageResult.java           # 分页响应封装
│       │   ├── Constants.java            # 常量定义
│       │   └── enums/                    # 枚举定义
│       │       ├── RoleEnum.java         # 角色枚举
│       │       ├── NotificationTypeEnum.java
│       │       ├── PostStatusEnum.java
│       │       └── ReportStatusEnum.java
│       ├── exception/                    # 异常处理
│       │   ├── BusinessException.java    # 业务异常
│       │   └── GlobalExceptionHandler.java
│       ├── security/                     # 安全模块
│       │   ├── JwtTokenProvider.java     # JWT 令牌提供者
│       │   ├── JwtAuthenticationFilter.java
│       │   ├── CasAuthenticationFilter.java
│       │   └── UserDetailsServiceImpl.java
│       ├── entity/                       # 数据库实体
│       │   ├── User.java
│       │   ├── Category.java
│       │   ├── Post.java
│       │   ├── Comment.java
│       │   ├── Like.java
│       │   ├── Favorite.java
│       │   ├── Share.java
│       │   ├── Notification.java
│       │   ├── Report.java
│       │   ├── AiResponse.java
│       │   ├── Role.java
│       │   └── Permission.java
│       ├── mapper/                       # MyBatis-Plus Mapper
│       ├── service/                      # 业务逻辑层
│       │   ├── UserService.java
│       │   ├── CategoryService.java
│       │   ├── PostService.java
│       │   ├── CommentService.java
│       │   ├── InteractionService.java
│       │   ├── NotificationService.java
│       │   ├── ReportService.java
│       │   ├── SearchService.java
│       │   ├── ContentFilterService.java
│       │   └── ai/                      # AI 回答服务
│       │       ├── AiService.java        # AI 接口调用
│       │       └── AiConfig.java         # AI 配置属性
│       ├── controller/                   # REST API 控制器
│       │   ├── AuthController.java       # 认证接口
│       │   ├── UserController.java       # 用户接口
│       │   ├── CategoryController.java   # 板块接口
│       │   ├── PostController.java       # 帖子接口
│       │   ├── CommentController.java    # 评论接口
│       │   ├── InteractionController.java# 互动接口
│       │   ├── NotificationController.java
│       │   ├── ReportController.java
│       │   ├── SearchController.java
│       │   ├── AiController.java         # AI 回答接口
│       │   └── AdminController.java      # 管理后台接口
│       ├── dto/                          # 请求/响应 DTO
│       │   ├── request/
│       │   └── response/
│       └── util/                         # 工具类
│           ├── SensitiveWordFilter.java  # 敏感词过滤
│           └── ValidationUtil.java
│   └── src/main/resources/
│       ├── application.yml
│       ├── application-dev.yml
│       └── db/
│           └── schema.sql                # 数据库初始化脚本
│
└── frontend/                             # React 前端
    ├── package.json
    ├── vite.config.ts
    ├── tsconfig.json
    └── src/
        ├── App.tsx
        ├── main.tsx
        ├── api/                          # API 请求封装
        │   ├── index.ts                  # Axios 实例与拦截器
        │   ├── auth.ts
        │   ├── user.ts
        │   ├── category.ts
        │   ├── post.ts
        │   ├── comment.ts
        │   ├── interaction.ts
        │   ├── notification.ts
        │   ├── report.ts
        │   ├── search.ts
        │   └── ai.ts
        ├── store/                        # Zustand 状态管理
        │   ├── useAuthStore.ts
        │   ├── usePostStore.ts
        │   └── useNotificationStore.ts
        ├── router/                       # 路由配置
        │   └── index.tsx
        ├── components/                   # 通用组件
        │   ├── Layout/                   # 页面布局
        │   │   ├── Header.tsx
        │   │   ├── Sidebar.tsx
        │   │   └── MainLayout.tsx
        │   ├── RichEditor/               # 富文本编辑器
        │   │   └── index.tsx
        │   ├── AIReplyCard/              # AI 回答卡片
        │   │   └── index.tsx
        │   ├── NotificationBell/         # 通知铃铛
        │   │   └── index.tsx
        │   ├── PostCard/                 # 帖子卡片
        │   │   └── index.tsx
        │   ├── CommentList/              # 评论列表
        │   │   └── index.tsx
        │   ├── LikeButton/               # 点赞按钮
        │   ├── FavoriteButton/           # 收藏按钮
        │   └── ShareButton/              # 分享按钮
        ├── pages/
        │   ├── Login/                    # 登录注册页
        │   │   └── index.tsx
        │   ├── Home/                     # 首页
        │   │   └── index.tsx
        │   ├── Post/                     # 帖子详情页
        │   │   └── index.tsx
        │   ├── NewPost/                  # 发帖/编辑页
        │   │   └── index.tsx
        │   ├── Profile/                  # 个人中心页
        │   │   └── index.tsx
        │   ├── Search/                   # 搜索页
        │   │   └── index.tsx
        │   ├── Notifications/            # 通知页
        │   │   └── index.tsx
        │   └── Admin/                    # 管理后台
        │       ├── index.tsx
        │       ├── UserManage/
        │       ├── CategoryManage/
        │       ├── PostManage/
        │       ├── ReportManage/
        │       └── SystemConfig/
        └── styles/
            └── global.css
```

---

## 二、数据库设计

### 2.1 用户表 t_user

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码（BCrypt加密） |
| nickname | VARCHAR(50) | | 昵称 |
| avatar | VARCHAR(500) | | 头像URL |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| phone | VARCHAR(20) | UNIQUE | 手机号 |
| bio | VARCHAR(500) | | 个人简介 |
| cas_id | VARCHAR(100) | UNIQUE | CAS统一身份标识 |
| status | TINYINT | DEFAULT 1 | 状态：0禁用 1正常 2禁言 |
| created_at | DATETIME | DEFAULT NOW | 注册时间 |
| updated_at | DATETIME | ON UPDATE NOW | 更新时间 |
| last_login_at | DATETIME | | 最后登录时间 |

### 2.2 角色表 t_role

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 角色ID |
| role_name | VARCHAR(50) | UNIQUE, NOT NULL | 角色名称 |
| role_code | VARCHAR(50) | UNIQUE, NOT NULL | 角色编码（ADMIN/MODERATOR/USER） |
| description | VARCHAR(200) | | 角色描述 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.3 用户角色关联表 t_user_role

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | ID |
| user_id | BIGINT | FK → t_user.id | 用户ID |
| role_id | BIGINT | FK → t_role.id | 角色ID |

### 2.4 权限表 t_permission

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 权限ID |
| permission_name | VARCHAR(100) | NOT NULL | 权限名称 |
| permission_code | VARCHAR(100) | UNIQUE, NOT NULL | 权限编码 |
| resource_type | VARCHAR(20) | | 资源类型（MENU/BUTTON/API） |
| parent_id | BIGINT | | 父权限ID |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.5 角色权限关联表 t_role_permission

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | ID |
| role_id | BIGINT | FK → t_role.id | 角色ID |
| permission_id | BIGINT | FK → t_permission.id | 权限ID |

### 2.6 板块表 t_category

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 板块ID |
| parent_id | BIGINT | DEFAULT 0 | 父板块ID（0为顶级） |
| name | VARCHAR(100) | NOT NULL | 板块名称 |
| icon | VARCHAR(500) | | 板块图标URL |
| description | VARCHAR(500) | | 板块描述 |
| sort_order | INT | DEFAULT 0 | 排序值（越小越靠前） |
| status | TINYINT | DEFAULT 1 | 状态：0禁用 1启用 |
| post_count | INT | DEFAULT 0 | 帖子数量（冗余） |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE NOW | 更新时间 |

### 2.7 帖子表 t_post

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 帖子ID |
| category_id | BIGINT | FK → t_category.id, NOT NULL | 板块ID |
| user_id | BIGINT | FK → t_user.id, NOT NULL | 作者ID |
| title | VARCHAR(200) | NOT NULL | 帖子标题 |
| content | LONGTEXT | NOT NULL | 帖子内容（富文本HTML） |
| summary | VARCHAR(500) | | 摘要（自动截取） |
| view_count | INT | DEFAULT 0 | 浏览量 |
| like_count | INT | DEFAULT 0 | 点赞数 |
| comment_count | INT | DEFAULT 0 | 评论数 |
| favorite_count | INT | DEFAULT 0 | 收藏数 |
| share_count | INT | DEFAULT 0 | 分享数 |
| is_top | TINYINT | DEFAULT 0 | 是否置顶：0否 1是 |
| is_essence | TINYINT | DEFAULT 0 | 是否加精：0否 1是 |
| ai_requested | TINYINT | DEFAULT 0 | 是否请求AI回答 |
| status | TINYINT | DEFAULT 1 | 状态：0草稿 1正常 2屏蔽 3删除 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE NOW | 更新时间 |

### 2.8 评论表 t_comment

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 评论ID |
| post_id | BIGINT | FK → t_post.id, NOT NULL | 帖子ID |
| user_id | BIGINT | FK → t_user.id, NOT NULL | 评论者ID |
| parent_id | BIGINT | DEFAULT 0 | 父评论ID（0为顶级评论） |
| reply_to_user_id | BIGINT | | 引用回复的目标用户ID |
| content | TEXT | NOT NULL | 评论内容 |
| like_count | INT | DEFAULT 0 | 点赞数 |
| status | TINYINT | DEFAULT 1 | 状态：0屏蔽 1正常 2删除 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.9 点赞表 t_like

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | ID |
| user_id | BIGINT | FK → t_user.id, NOT NULL | 用户ID |
| target_id | BIGINT | NOT NULL | 目标ID（帖子ID/评论ID） |
| target_type | TINYINT | NOT NULL | 目标类型：1帖子 2评论 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

**唯一约束**: UNIQUE(user_id, target_id, target_type)

### 2.10 收藏表 t_favorite

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | ID |
| user_id | BIGINT | FK → t_user.id, NOT NULL | 用户ID |
| post_id | BIGINT | FK → t_post.id, NOT NULL | 帖子ID |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

**唯一约束**: UNIQUE(user_id, post_id)

### 2.11 分享记录表 t_share

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | ID |
| user_id | BIGINT | FK → t_user.id | 用户ID |
| post_id | BIGINT | FK → t_post.id, NOT NULL | 帖子ID |
| channel | VARCHAR(50) | | 分享渠道（WECHAT/LINK/COPY等） |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.12 消息通知表 t_notification

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 通知ID |
| receiver_id | BIGINT | FK → t_user.id, NOT NULL | 接收用户ID |
| sender_id | BIGINT | FK → t_user.id | 发送用户ID（系统通知为空） |
| type | TINYINT | NOT NULL | 类型：1评论 2点赞 3收藏 4系统 5AI回答完成 6举报处理 |
| title | VARCHAR(200) | | 通知标题 |
| content | VARCHAR(500) | | 通知内容 |
| related_id | BIGINT | | 关联ID（帖子ID/评论ID等） |
| is_read | TINYINT | DEFAULT 0 | 是否已读：0未读 1已读 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.13 举报表 t_report

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 举报ID |
| reporter_id | BIGINT | FK → t_user.id, NOT NULL | 举报人ID |
| target_id | BIGINT | NOT NULL | 目标ID |
| target_type | TINYINT | NOT NULL | 目标类型：1帖子 2评论 3用户 |
| reason | VARCHAR(500) | NOT NULL | 举报原因 |
| status | TINYINT | DEFAULT 0 | 状态：0待处理 1已屏蔽 2已忽略 3已警告 |
| handler_id | BIGINT | FK → t_user.id | 处理人ID |
| handle_result | VARCHAR(500) | | 处理结果说明 |
| handled_at | DATETIME | | 处理时间 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.14 AI回答记录表 t_ai_response

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | ID |
| post_id | BIGINT | FK → t_post.id, UNIQUE, NOT NULL | 帖子ID |
| content | LONGTEXT | | AI回答内容（Markdown） |
| model | VARCHAR(100) | | 使用的AI模型名称 |
| prompt_tokens | INT | | 提示Token消耗 |
| completion_tokens | INT | | 补全Token消耗 |
| status | TINYINT | DEFAULT 0 | 状态：0生成中 1已完成 2失败 |
| error_message | VARCHAR(500) | | 错误信息 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE NOW | 更新时间 |

---

## 三、API接口设计

### 3.1 认证模块 AuthController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/auth/register | 用户注册 | 公开 |
| POST | /api/auth/login | 用户登录（返回JWT） | 公开 |
| GET | /api/auth/cas-login | CAS统一登录（重定向到CAS） | 公开 |
| GET | /api/auth/cas-callback | CAS登录回调（验证Ticket） | 公开 |
| POST | /api/auth/refresh | 刷新Token | 已登录 |
| POST | /api/auth/logout | 退出登录 | 已登录 |

**注册请求体**:
```json
{
  "username": "string",
  "password": "string",
  "email": "string",
  "nickname": "string"
}
```

**登录请求体**:
```json
{
  "username": "string",
  "password": "string"
}
```

**登录响应体**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiJ9...",
    "expiresIn": 86400,
    "user": {
      "id": 1,
      "username": "zhangsan",
      "nickname": "张三",
      "avatar": "https://...",
      "roles": ["USER"]
    }
  }
}
```

### 3.2 用户模块 UserController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/user/profile | 获取当前用户信息 | 已登录 |
| PUT | /api/user/profile | 修改个人信息 | 已登录 |
| PUT | /api/user/password | 修改密码 | 已登录 |
| POST | /api/user/avatar | 上传头像 | 已登录 |
| GET | /api/user/{id} | 获取指定用户公开信息 | 已登录 |
| GET | /api/user/posts | 我的帖子列表（分页） | 已登录 |
| GET | /api/user/comments | 我的评论列表（分页） | 已登录 |
| GET | /api/user/favorites | 我的收藏列表（分页） | 已登录 |

### 3.3 板块模块 CategoryController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/categories/tree | 获取板块树形列表 | 公开 |
| GET | /api/categories/{id} | 获取板块详情 | 公开 |
| POST | /api/categories | 创建板块 | 管理员 |
| PUT | /api/categories/{id} | 修改板块 | 管理员 |
| DELETE | /api/categories/{id} | 删除板块 | 管理员 |
| PUT | /api/categories/{id}/sort | 修改排序 | 管理员 |
| PUT | /api/categories/{id}/status | 启用/禁用板块 | 管理员 |

### 3.4 帖子模块 PostController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/posts | 发布帖子 | 已登录 |
| PUT | /api/posts/{id} | 编辑帖子 | 作者/管理员 |
| DELETE | /api/posts/{id} | 删除帖子 | 作者/管理员 |
| GET | /api/posts/{id} | 帖子详情（含AI回答） | 已登录 |
| GET | /api/posts | 帖子列表（分页/筛选/排序） | 公开 |
| PUT | /api/posts/{id}/top | 置顶/取消置顶 | 管理员 |
| PUT | /api/posts/{id}/essence | 加精/取消加精 | 管理员 |

**发帖请求体**:
```json
{
  "categoryId": 1,
  "title": "关于Spring Boot 3.2的新特性讨论",
  "content": "<p>富文本HTML内容...</p>",
  "aiRequested": true
}
```

**帖子列表查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| categoryId | Long | 板块ID筛选 |
| keyword | String | 关键词搜索 |
| sortBy | String | 排序方式：LATEST/HOT/TOP |
| isTop | Boolean | 仅看置顶 |
| isEssence | Boolean | 仅看加精 |
| authorId | Long | 指定作者 |
| page | Integer | 页码（默认1） |
| size | Integer | 每页条数（默认20） |

**帖子详情响应体**:
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "categoryId": 1,
    "categoryName": "后端开发",
    "author": {
      "id": 1,
      "username": "zhangsan",
      "nickname": "张三",
      "avatar": "https://..."
    },
    "title": "关于Spring Boot 3.2的新特性讨论",
    "content": "<p>富文本HTML内容...</p>",
    "viewCount": 128,
    "likeCount": 15,
    "commentCount": 8,
    "favoriteCount": 5,
    "isTop": false,
    "isEssence": true,
    "aiRequested": true,
    "aiResponse": {
      "content": "关于Spring Boot 3.2的新特性...",
      "model": "gpt-4o-mini",
      "status": 1,
      "createdAt": "2026-05-06T10:30:00"
    },
    "isLiked": false,
    "isFavorited": true,
    "createdAt": "2026-05-06T10:00:00",
    "updatedAt": "2026-05-06T10:00:00"
  }
}
```

### 3.5 评论模块 CommentController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/comments | 发布评论 | 已登录 |
| DELETE | /api/comments/{id} | 删除评论 | 作者/管理员 |
| GET | /api/comments/post/{postId} | 帖子评论列表（分页） | 已登录 |

**评论请求体**:
```json
{
  "postId": 1,
  "parentId": 0,
  "replyToUserId": null,
  "content": "非常好的分享！"
}
```

**引用回复请求体**:
```json
{
  "postId": 1,
  "parentId": 5,
  "replyToUserId": 3,
  "content": "同意你的观点，补充一下..."
}
```

### 3.6 互动模块 InteractionController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/interaction/like | 点赞（幂等） | 已登录 |
| DELETE | /api/interaction/like | 取消点赞 | 已登录 |
| POST | /api/interaction/favorite | 收藏（幂等） | 已登录 |
| DELETE | /api/interaction/favorite | 取消收藏 | 已登录 |
| POST | /api/interaction/share | 记录分享 | 已登录 |

**点赞请求体**:
```json
{
  "targetId": 1,
  "targetType": 1
}
```

### 3.7 搜索模块 SearchController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/search | 搜索帖子 | 公开 |
| GET | /api/search/hot | 热门搜索词 | 公开 |

**搜索查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| keyword | String | 搜索关键词 |
| categoryId | Long | 板块筛选 |
| sortBy | String | 排序：RELEVANCE/LATEST/HOT |
| startTime | String | 起始时间 |
| endTime | String | 结束时间 |
| page | Integer | 页码 |
| size | Integer | 每页条数 |

### 3.8 通知模块 NotificationController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/notifications | 通知列表（分页） | 已登录 |
| GET | /api/notifications/unread-count | 未读通知数量 | 已登录 |
| PUT | /api/notifications/{id}/read | 标记已读 | 已登录 |
| PUT | /api/notifications/read-all | 全部标记已读 | 已登录 |

### 3.9 举报模块 ReportController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/reports | 提交举报 | 已登录 |
| GET | /api/reports | 举报列表 | 管理员 |
| PUT | /api/reports/{id}/handle | 处理举报 | 管理员 |

**举报请求体**:
```json
{
  "targetId": 1,
  "targetType": 1,
  "reason": "内容包含违规信息"
}
```

**举报处理请求体**:
```json
{
  "status": 1,
  "handleResult": "确认违规，已屏蔽内容"
}
```

### 3.10 AI回答模块 AiController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/ai/generate/{postId} | 触发AI回答 | 帖主 |
| POST | /api/ai/retry/{postId} | 重新生成AI回答 | 帖主 |
| GET | /api/ai/response/{postId} | 获取AI回答内容 | 已登录 |
| GET | /api/ai/stream/{postId} | SSE流式获取AI回答 | 已登录 |

### 3.11 管理后台模块 AdminController

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/admin/users | 用户列表（分页/搜索） | 管理员 |
| PUT | /api/admin/users/{id}/status | 启用/禁用用户 | 管理员 |
| PUT | /api/admin/users/{id}/role | 修改用户角色 | 管理员 |
| GET | /api/admin/posts | 帖子管理列表 | 管理员 |
| PUT | /api/admin/posts/{id}/status | 修改帖子状态 | 管理员 |
| GET | /api/admin/stats | 系统统计数据 | 管理员 |
| GET | /api/admin/config | 获取系统配置 | 管理员 |
| PUT | /api/admin/config | 修改系统配置 | 管理员 |
| GET | /api/admin/sensitive-words | 获取敏感词库 | 管理员 |
| PUT | /api/admin/sensitive-words | 更新敏感词库 | 管理员 |

---

## 四、前端页面设计

### 4.1 页面清单

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | /login | 账号密码登录 + CAS登录入口 |
| 注册页 | /register | 用户注册表单 |
| 首页 | / | 板块导航 + 帖子列表 + 排序筛选 |
| 帖子详情页 | /post/:id | 帖子内容 + AI回答 + 评论列表 + 互动 |
| 发帖页 | /post/new | 富文本编辑器 + 板块选择 + AI回答开关 |
| 编辑帖子页 | /post/:id/edit | 编辑已有帖子 |
| 个人中心页 | /profile | 个人信息 + 我的帖子/评论/收藏 |
| 搜索页 | /search | 搜索结果 + 筛选面板 |
| 通知页 | /notifications | 通知列表 + 已读/未读筛选 |
| 管理后台-首页 | /admin | 系统统计概览 |
| 管理后台-用户管理 | /admin/users | 用户列表 + 角色管理 |
| 管理后台-板块管理 | /admin/categories | 板块树形管理 |
| 管理后台-帖子管理 | /admin/posts | 帖子列表 + 置顶/加精/屏蔽 |
| 管理后台-举报管理 | /admin/reports | 举报审核列表 |
| 管理后台-系统配置 | /admin/config | AI配置 + 敏感词 + CAS配置 |

### 4.2 核心页面交互流程

#### 4.2.1 用户注册登录流程
```
用户访问 → 登录页
  ├─ 账号密码登录 → 输入用户名/密码 → 验证 → 返回JWT → 跳转首页
  ├─ CAS统一登录 → 重定向到CAS → CAS认证 → 回调验证 → 创建/关联本地用户 → 返回JWT → 跳转首页
  └─ 注册新账号 → 填写注册信息 → 校验 → 注册成功 → 自动登录 → 跳转首页
```

#### 4.2.2 发帖与AI回答流程
```
用户点击"发帖" → 进入发帖页
  → 填写标题 + 富文本内容
  → 选择板块（级联选择）
  → 勾选"请求AI回答"开关
  → 点击"发布"
    → 帖子保存到数据库
    → 若勾选AI回答：
      → 后端异步调用AI接口
      → AI回答生成中：帖子详情页一楼显示"AI正在思考..."加载动画
      → AI回答完成：内容存入t_ai_response表
      → 通过WebSocket/SSE推送完成通知
      → 帖子详情页一楼AI回答卡片更新为完整内容
    → 跳转到帖子详情页
```

#### 4.2.3 帖子详情页交互流程
```
用户进入帖子详情页
  → 加载帖子内容（富文本渲染）
  → 加载一楼AI回答（若有，特殊样式展示）
  → 加载评论列表（支持嵌套/引用回复）
  → 互动操作：
    ├─ 点赞（按钮动画 + 计数更新）
    ├─ 收藏（按钮状态切换 + 计数更新）
    ├─ 分享（复制链接 / 生成海报）
    ├─ 评论（输入框 + 引用回复）
    └─ 举报（弹窗选择原因）
  → 帖主额外操作：
    ├─ 编辑帖子
    ├─ 删除帖子
    └─ 重新生成AI回答（若AI回答失败）
```

#### 4.2.4 举报审核流程
```
用户点击"举报" → 选择举报原因 → 提交举报
  → 管理员在后台看到举报列表
  → 管理员查看举报详情
  → 管理员处理：
    ├─ 屏蔽：目标内容状态改为"屏蔽"，通知被举报人
    ├─ 忽略：举报标记为"已忽略"，不处理内容
    └─ 警告：通知被举报人，内容暂不屏蔽
  → 举报人收到处理结果通知
```

### 4.3 AI回答卡片UI设计

```
┌──────────────────────────────────────────────────┐
│  🤖 AI 助手回答                          GPT-4o  │
│──────────────────────────────────────────────────│
│                                                  │
│  关于Spring Boot 3.2的新特性，以下是主要更新：     │
│                                                  │
│  1. **虚拟线程支持**：Spring Boot 3.2 原生支持    │
│     Java 21的虚拟线程...                          │
│                                                  │
│  2. **RestClient支持**：新的声明式HTTP客户端...    │
│                                                  │
│  3. **Observability增强**：Micrometer集成...      │
│                                                  │
│──────────────────────────────────────────────────│
│  🔄 重新生成（仅帖主可见）    2026-05-06 10:30    │
└──────────────────────────────────────────────────┘
```

- AI回答卡片使用渐变边框（蓝紫色渐变）与普通评论区分
- 左上角显示AI机器人图标 + "AI 助手回答"标签
- 右上角显示使用的AI模型名称
- 内容支持Markdown渲染
- 底部显示生成时间，帖主可见"重新生成"按钮

---

## 五、后端核心逻辑设计

### 5.1 认证与授权

```
请求进入 → JwtAuthenticationFilter
  ├─ 无Token → 放行（由Security判断是否需要认证）
  ├─ 有Token → 解析验证 → 设置SecurityContext → 放行
  └─ Token过期 → 返回401 → 前端跳转登录页

CAS登录流程：
  用户点击"CAS登录" → 后端重定向到CAS_SERVER/login?service=callback_url
  → 用户在CAS完成认证 → CAS重定向回callback_url?ticket=xxx
  → 后端验证ticket → 获取CAS用户信息
  → 查找本地用户(cas_id匹配)
    ├─ 已存在 → 生成JWT返回
    └─ 不存在 → 自动创建本地用户 → 生成JWT返回
```

### 5.2 AI回答服务

```java
// AiService 核心逻辑
public void generateAiResponse(Long postId) {
    Post post = postMapper.selectById(postId);
    
    // 1. 创建AI回答记录（状态：生成中）
    AiResponse aiResponse = new AiResponse();
    aiResponse.setPostId(postId);
    aiResponse.setStatus(0); // 生成中
    aiResponseMapper.insert(aiResponse);
    
    // 2. 异步调用AI接口
    CompletableFuture.runAsync(() -> {
        try {
            String prompt = buildPrompt(post.getTitle(), post.getContent());
            String aiContent = callAiApi(prompt);
            
            // 3. 内容合规校验
            String filteredContent = contentFilterService.filter(aiContent);
            
            // 4. 更新AI回答记录
            aiResponse.setContent(filteredContent);
            aiResponse.setStatus(1); // 已完成
            aiResponseMapper.updateById(aiResponse);
            
            // 5. 发送通知给帖主
            notificationService.sendAiCompleteNotification(post.getUserId(), postId);
            
            // 6. WebSocket推送
            webSocketService.sendToUser(post.getUserId(), "AI回答已完成");
            
        } catch (Exception e) {
            aiResponse.setStatus(2); // 失败
            aiResponse.setErrorMessage(e.getMessage());
            aiResponseMapper.updateById(aiResponse);
        }
    });
}

// SSE流式响应
public SseEmitter streamAiResponse(Long postId) {
    SseEmitter emitter = new SseEmitter(60000L);
    // 流式调用AI接口，逐字推送给前端
    // ...
    return emitter;
}
```

### 5.3 内容合规校验

```java
// SensitiveWordFilter - DFA算法敏感词过滤
public class SensitiveWordFilter {
    private Map<Character, Object> sensitiveWordMap;
    
    // 初始化敏感词树
    public void initSensitiveWordMap(Set<String> words) { ... }
    
    // 检测是否包含敏感词
    public boolean containsSensitiveWord(String text) { ... }
    
    // 过滤敏感词（替换为***）
    public String filter(String text) { ... }
}
```

### 5.4 通知服务

```
通知触发场景：
1. 帖子被评论 → 通知帖主
2. 评论被引用回复 → 通知被引用用户
3. 帖子/评论被点赞 → 通知作者
4. 帖子被收藏 → 通知帖主
5. AI回答完成 → 通知帖主
6. 举报被处理 → 通知举报人和被举报人
7. 系统公告 → 通知所有用户

通知推送方式：
- 在线用户：WebSocket实时推送
- 离线用户：下次登录时拉取未读通知
```

---

## 六、任务分解与排期

### 阶段一：项目基础架构搭建（任务 1.1 ~ 1.3）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 1.1 | 后端项目初始化 | Spring Boot项目骨架、pom.xml、application.yml、统一响应封装、全局异常处理、跨域配置 |
| 1.2 | 数据库设计与初始化 | schema.sql（14张表）、MyBatis-Plus实体类、Mapper接口 |
| 1.3 | 前端项目初始化 | Vite+React+TS项目、Ant Design配置、路由结构、Axios封装、布局组件 |

### 阶段二：用户与认证模块（任务 2.1 ~ 2.4）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 2.1 | 后端用户认证API | 注册/登录/刷新Token/退出接口、JWT工具类、Spring Security配置 |
| 2.2 | 后端CAS统一登录 | CAS Client集成、Ticket验证、自动创建/关联本地用户 |
| 2.3 | 后端个人中心API | 个人信息CRUD、头像上传、我的帖子/评论/收藏列表 |
| 2.4 | 前端登录注册页面 | 登录页（含CAS入口）、注册页、Token管理、路由守卫 |
| 2.5 | 前端个人中心页面 | 个人信息编辑、头像上传、我的帖子/评论/收藏Tab |

### 阶段三：板块与帖子核心模块（任务 3.1 ~ 3.5）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 3.1 | 后端板块管理API | 板块树形列表、CRUD、排序、启用/禁用 |
| 3.2 | 后端帖子管理API | 帖子发布/编辑/删除、详情、列表（分页/筛选/排序）、置顶/加精 |
| 3.3 | 前端板块与帖子列表 | 首页板块导航、帖子列表卡片、排序筛选、面包屑导航 |
| 3.4 | 前端帖子详情页 | 富文本渲染、元信息展示、置顶/加精标识 |
| 3.5 | 前端发帖/编辑页 | 富文本编辑器、板块级联选择、AI回答开关、表单校验 |

### 阶段四：互动模块（任务 4.1 ~ 4.2）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 4.1 | 后端互动API | 点赞/取消点赞、评论发布/删除、收藏/取消收藏、分享记录 |
| 4.2 | 前端互动组件 | 点赞按钮、评论列表（嵌套/引用）、评论输入框、收藏按钮、分享按钮 |

### 阶段五：AI回答模块（任务 5.1 ~ 5.2）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 5.1 | 后端AI回答服务 | AI接口调用、异步生成、SSE流式响应、内容合规校验、失败重试 |
| 5.2 | 前端AI回答展示 | AI回答卡片组件、加载动画、Markdown渲染、重新生成按钮 |

### 阶段六：搜索与筛选模块（任务 6.1 ~ 6.2）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 6.1 | 后端搜索API | 关键词搜索、高级筛选、热门搜索词、搜索结果高亮 |
| 6.2 | 前端搜索页面 | 搜索栏、搜索结果页、筛选面板、热门搜索标签 |

### 阶段七：消息通知模块（任务 7.1 ~ 7.2）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 7.1 | 后端通知API | 通知生成逻辑、通知列表、已读标记、未读数量、WebSocket推送 |
| 7.2 | 前端通知组件 | 通知铃铛、通知下拉面板、通知列表页、类型图标区分 |

### 阶段八：权限与内容管理模块（任务 8.1 ~ 8.3）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 8.1 | 后端权限管理 | RBAC模型、接口权限注解、内容合规校验服务、举报处理 |
| 8.2 | 后端后台管理API | 用户管理、板块管理、帖子管理、举报管理、系统配置 |
| 8.3 | 前端后台管理页面 | 管理后台布局、用户/板块/帖子/举报管理页、系统配置页 |

### 阶段九：集成与优化（任务 9.1 ~ 9.3）

| 编号 | 任务 | 交付物 |
|------|------|--------|
| 9.1 | 系统集成 | 前后端联调、CAS全流程、AI回答全流程、WebSocket联调 |
| 9.2 | 性能与安全优化 | Redis缓存、接口限流、XSS/SQL注入防护、幂等性处理 |
| 9.3 | 测试验证 | 核心流程测试、管理流程测试、并发测试、边界场景测试 |

---

## 七、任务依赖关系

```
阶段一（基础架构）
  │
  ├─→ 阶段二（用户认证）
  │      │
  │      ├─→ 阶段三（板块帖子）──→ 阶段四（互动）
  │      │                           │
  │      │                           └─→ 阶段七（消息通知）
  │      │
  │      ├─→ 阶段五（AI回答）←── 阶段三
  │      │
  │      ├─→ 阶段六（搜索筛选）←── 阶段三
  │      │
  │      └─→ 阶段八（权限管理）
  │              │
  │              └─→ 阶段九（集成优化）←── 所有模块
  │
  └────────────────────────────────────────────────────
```

**可并行开发的模块**：
- 阶段四（互动）与 阶段五（AI回答）与 阶段六（搜索筛选）可并行
- 前后端可并行开发（约定API接口后）

---

## 八、关键配置项

### 8.1 application.yml 核心配置

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tech_community?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD}
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000
  refresh-expiration: 604800000

cas:
  server-url: https://cas.example.com/cas
  client-service: http://localhost:8080/api/auth/cas-callback

ai:
  base-url: https://api.openai.com
  api-key: ${AI_API_KEY}
  model: gpt-4o-mini
  max-tokens: 2000
  temperature: 0.7

content-filter:
  enabled: true
  replacement: "***"

rate-limit:
  enabled: true
  post-limit: 10/hour
  comment-limit: 30/hour
```

### 8.2 前端环境配置

```env
VITE_API_BASE_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws
VITE_UPLOAD_MAX_SIZE=10485760
```

---

## 九、验收标准

| 模块 | 验收标准 |
|------|---------|
| 用户认证 | 注册/登录/CAS登录全流程可用，Token刷新机制正常，路由守卫生效 |
| 板块管理 | 多级板块树形展示正确，CRUD操作正常，排序生效 |
| 帖子管理 | 富文本发帖/编辑/删除正常，列表分页排序筛选正确，置顶加精生效 |
| 互动功能 | 点赞/评论/收藏/分享操作正常，计数实时更新，引用回复展示正确 |
| AI回答 | 发帖勾选AI回答后一楼正确展示AI内容，流式加载体验良好，失败可重试 |
| 搜索筛选 | 关键词搜索结果正确，高级筛选组合生效，搜索结果高亮 |
| 消息通知 | 各场景通知正确生成，WebSocket实时推送，已读/未读管理正常 |
| 权限管理 | RBAC权限控制生效，敏感词过滤正常，举报审核流程完整 |
| 管理后台 | 用户/板块/帖子/举报管理功能完整，系统配置可修改生效 |
| 性能安全 | 接口限流生效，XSS/SQL注入防护有效，并发场景下数据一致 |
